  <tr>
    <th>Title</th>
    <th>Recipe</th>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @foods.each do |food| %>
  <tr>
    <td><%= food.title %></td>
    <td><%= food.recipe %></td>
    <td><%= food.category.title %></td>
    <td><%= link_to 'Show', food %></td>
    <td><%= link_to 'Edit', edit_food_path(food) %></td>
    <td><%= link_to 'Destroy', food, :method => :delete,
                       :remote => true,
                       :confirm => 'Are you sure?',
                       :class => 'remote_del',
                       :'data-container' => 'foods-list' %></td>
  </tr>
<% end %>