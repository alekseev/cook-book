<p id="notice"><%= notice %></p>

<p>
  <b>Название:</b>
  <%= @category.title %> (<%= link_to 'редактировать', edit_category_path(@category) %>)
</p>

<h3>Список блюд:</h3>
<table id="foods-list">
<%= render :partial => 'foods/foods', :locals => {:foods => @category.foods} %>
</table>
<%= link_to 'Добавить блюдо', new_food_path(:category => @category.id) %>